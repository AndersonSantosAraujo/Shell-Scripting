### Sintaxe
`for (( atrib_variável; condição; processo_iteração ))`

### Exemplo
`#!/bin/bash`
`#Exemplo de comando for no estilo linguagem C`

`for (( i=1; i<=15; i++ ))`
`do`
	`echo "Número: $i"`
`done`
